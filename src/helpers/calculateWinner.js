import winRules from '../../rules/rules.json';

export default function calculateWinner(squares) {
	console.log(squares);
	// for (let i = 0; i < lines.length; i++) {
	// 	const [a, b, c] = lines[i];
	// 	if (squares[a] && squares[a] === squares[b] && squares[a] === squares[c]) {
	// 		return squares[a];
	// 	}
	// }
	return null;
}